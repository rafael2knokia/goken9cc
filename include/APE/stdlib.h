/* Empty: every benchs/compcert/*.c program wired up so far also
 * includes <stdio.h>, which carries everything stdlib.h would
 * otherwise declare (atoi/atol/malloc/calloc/free). See stdio.h's own
 * comment.
 */
