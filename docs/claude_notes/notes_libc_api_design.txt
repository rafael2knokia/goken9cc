libc.h API design philosophy: Plan9-clean primitives vs POSIX-compat doors
============================================================================

claude: Not tied to one syscall tier the way plan_syscalls.txt/
notes_libc_selfhost.txt are -- this is the recurring *design* question
that comes up every time a Plan9-clean primitive (goken's libc.h is
modeled on Plan9's, which the project considers the cleanest OS API
out there) meets an OS that doesn't have the same clean shape natively
(Linux, macOS, Windows). Read this before deciding whether a new gap
needs a POSIX-habit door built alongside the clean primitive, needs
the clean primitive refused/kept Plan9-only, or needs something in
between. Grown out of a survey done while scoping Tier 6 (notify/
noted/atnotify/postnote, see plan_syscalls.txt) -- that section's own
open design question is what prompted writing this down as a reusable
framework instead of a one-off aside.

The categories, from actual precedent already in the tree
------------------------------------------------------------------------

Every real case so far falls into one of these. They are not equally
desirable -- A and B are the ones that actually preserve the clean
API's primacy while still letting POSIX-habituated callers in; C/D/E
are "no real compromise was needed here"; F/G are open or refused.

A. General Plan9 primitive kept, POSIX-habit convenience wrapper added
   on top, both fully live:
   - rfork(int) (general, flag-based) stays; fork(void) is a one-line
     wrapper: rfork(RFPROC|RFFDG|RFREND). See os/plan9/fork.c.
   - wait(void) returning a rich Waitmsg (pid + time + free-text msg)
     stays; waitpid(void) is the dumbed-down POSIX-habit door in.
   - exits(char*)/_exits(char*) stay; exit(int) sits right alongside
     with no steering comment at all -- the most permissive instance
     of this pattern in the tree.

B. Unix door kept, but explicitly discouraged in favor of the clean
   Plan9 one:
   - brk(void*) is declared, but os/mem.h says outright: "Do not use
     brk() because it is not portable and does not really work on
     macOS and Windows. Use sbrk() instead." Every internal caller
     (malloc) is steered to sbrk(). This is the one direct textual
     precedent for "provide it, but steer people off it."

C. Unix door deliberately refused -- no compat API was built because
   nothing real needed one:
   - os/stat.h explicitly does NOT expose Plan9's raw wire-format
     stat(char*,uchar*,int)/fstat portably. Comment: that byte-buffer
     shape "is only meaningful on Plan9, where the kernel's own
     FSTAT/FWSTAT syscalls genuinely speak it." Every actual caller in
     this tree (linkers/ar/ar.c, debuggers/acid/) only ever wants the
     Dir* struct dirstat()/dirfstat() already return.

D. Unix concept present, but scoped narrowly to one internal need,
   never elevated to a general program-facing API:
   - errno/ERANGE/strerror exist ONLY because fmt/fltfmt.c and
     fmt/strtod.c need ERANGE internally when a parsed float
     overflows. os/posix/errno.h says so explicitly. Unlike exit(),
     nobody decided errno deserved first-class status alongside
     errstr()/werrstr()/rerrstr() -- it is not a general compat API,
     just a leak-proofed internal detail.

E. Just relabeling, not a real compromise:
   - create()/remove() are creat()/unlink() under Plan9 spelling --
     identical semantics, no dual API needed, no design decision to
     make.

F. Genuinely unresolved -- open questions on record elsewhere:
   - Notes vs. signals: see the "Notes vs. signals" section below.

RESOLVED since the above was first written: Environment (getenv/putenv/environ)
------------------------------------------------------------------------

Plan9's environment is literally files under /env/* (readable/
writable/watchable, per-namespace, no in-memory array at all --
os/plan9/getenv.c's own comment: "there is no environ array to walk").
mk/rc's own readenv()/exportenv() enumerate it today via dirread() on
/env directly (mk/Plan9.c) -- real code, but Plan9-only, not portable
to any other GOOS mk might self-host on.

The decision, when it actually came up: do NOT build a Plan9-shaped
"enumerate a virtual /env directory" abstraction for the other GOOSes.
getenv()/putenv() (already declared in os/env.h, plain POSIX shape)
are a fine enough primitive on their own merits -- the only real gap
was that nothing could enumerate every variable at once, which is what
mk's own readenv()/exportenv() actually need dirread(/env) for. So the
fix was narrower than a new abstraction: expose environ() (include/os/
env.h), a nil-terminated char** of "NAME=value" strings -- port/
getenv.c already computed exactly this walk internally (from
_mainargv, past argv's own nil, matching every POSIX-shaped GOOS'
process-startup contract) for getenv()'s own use, just never exposed
it. A caller that wants "every variable" now gets a plain array to
walk, no directory-listing/open-per-file dance needed the way Plan9's
own /env forces even on Plan9 itself.

putenv() got a REAL implementation for the first time anywhere in this
tree alongside this (port/putenv.c, linux+darwin, sharing port/
getenv.c's own "POSIX-shaped GOOSes reach the environment identically"
placement) -- declared since before this session but never backed by
any code. Can't mutate the kernel-provided block in place (no slack
around it on the initial stack), so it allocates a fresh array each
call and repoints port/getenv.c's own _environp at it, same "switch to
a new block, don't edit the original" shape a real POSIX putenv()/
setenv() has internally.

UPDATE: plan9 and windows now have real putenv() implementations too
(os/plan9/putenv.c, ported as-is from principia's own 9sys/putenv.c --
create(path, OWRITE, perm) truncates an existing file exactly the way
POSIX's OTRUNC would, so "add" and "replace" need no separate branch
the way port/putenv.c's own POSIX-side implementation does; os/windows/
putenv.c over a new _winsetenv/SetEnvironmentVariableA stub in
winio_amd64.s, the write-side counterpart of the existing _wingetenv).
Adding SetEnvironmentVariableA to linkers/lk/pe.c's own importfuncs[]
table shifted the import-table byte layout of every PE binary 6l
produces, INCLUDING tests/s/variants' own golden-diffed pe_amd64.exe --
the exact same regression already on record in this file's own Tier 4
windows-round entry for the same reason (four different new imports
that round); fixed the same documented way, `mk pe_amd64.mkgoldpe` to
regenerate the golden, not by avoiding new imports.

Real, unplanned finding along the way, from actually trying to RUN the
new os/plan9/putenv.c under machines/5i (not just build-checking it,
which is all env.exe -- the existing test exercising getenv() this
same way -- had ever gotten before): /env access does not work under
5i/vi AT ALL, for read OR write. Confirmed pre-existing, not something
this round introduced: a bare getenv("PATH") (untouched code) also
returns nil there. Root cause: machines/5i/vi's own sysopen()/
syscreate() just call the HOST's real open()/create() with the Plan9
path string passed through literally (the same "calls the host's real
syscall with the same path" design pipe.c's own comment already
documents as deliberate for real files/pipes) -- but there is no real
/env directory on the host filesystem for that to resolve to, unlike
a genuine Plan9 kernel's in-kernel synthesized /env. Same category of
gap as the notify()/noted() one already on record (a real VM-level
limitation, not a libc bug) -- accepted and documented rather than
fixed, extending 5i/vi to synthesize a real /env filesystem being a
separate, nontrivial emulator feature, not something this round's
scope covered. Both os/plan9/getenv.c (pre-existing) and os/plan9/
putenv.c (new) are therefore build-verified only on plan9, same
"kept buildable, not runtime-verified" treatment as fork.exe/stat.exe/
dirread.exe already have there.

mk/rc's own readenv()/exportenv() have NOT been rewired to use
environ()/getenv()/putenv() instead of raw /env dirread() -- that was
explicitly out of scope when this was implemented ("let's not modify
mk for now, just add the API"), left for whenever mk/rc self-hosting
actually gets picked up.

G. No Unix analog exists at all -- not even attempted, and probably
   shouldn't be:
   - os/plan9/namespace.h: bind()/mount()/unmount(), Plan9's
     per-process private namespaces. Linux's mount namespaces are a
     completely different (global, privileged) mechanism -- there is
     no lossy-but-usable translation the way notes->signals at least
     partially has one. Left untouched, correctly.
   - os/plan9/segment.h: segattach()/segbrk()/segdetach() -- Plan9
     segment-based memory, loosely mmap()-shaped but not surveyed or
     attempted. Same category of future problem as notes/signals, not
     designed yet.

BOOT/lib9/: a general precedent source for this whole category of decisions
------------------------------------------------------------------------

Worth checking BEFORE designing any future Plan9-vs-Unix gap, not just
notify/signals: BOOT/lib9/ (plan9port's own Plan9-on-Unix compat
library, vendored here as this project's host-bootstrap reference C
library -- the same status the "reference implementations already
exist in BOOT/lib9/" line already carries in plan_syscalls.txt's Tier
4 section) is a decades-proven, load-bearing answer to almost every
category above, for real programs on real Unix systems. Three examples
beyond notify.c/atnotify.c (already covered below), each refining or
corroborating a specific category:

- errstr.c (category D): lib9's errstr()/werrstr()/rerrstr() bridge to
  REAL Unix errno, not just an internal-only detail -- a sentinel value
  (EPLAN9) marks errno when a caller has explicitly set a Plan9-style
  string via werrstr(); rerrstr() falls back to strerror(errno) only
  when that sentinel ISN'T set, i.e. when a raw Unix syscall failure
  is what actually happened. This is the real target design goken's
  own errstr() doesn't implement yet -- and lib_core/libc/port/
  errstr.c's own header comment already says exactly why, independent
  of this survey: "NOT bridged from POSIX errno/strerror()... nothing
  in this tree's syscall layer ever actually sets the global errno on
  a failed call... bridging from it here would silently return an
  empty string on every real syscall failure anyway... a real,
  separate gap... tracked in todo.org". So this isn't a missed
  precedent, it's a correctly-deferred one -- lib9's errstr.c is the
  ready reference the day the syscall layer starts populating errno.

- rfork.c (category A, cautionary): the inverse direction from what
  might be assumed. lib9 does NOT get to keep the general primitive on
  the "hard" platform the way this project's own fork()-wraps-rfork()
  does -- it emulates the GENERAL p9rfork() on top of plain fork()/
  wait4()/pipe()/setpgid(), including a genuinely gnarly double-fork-
  plus-pipe-handshake ("pipe dance", the file's own name for it) just
  to emulate RFNOWAIT, and still outright refuses true RFPROC-without-
  RFFDG (shared-memory rfork): "cannot use rfork for shared memory --
  use libthread". Read as validation, not just parallel precedent: it
  shows that attempting a full general-primitive emulation on the
  platform that lacks it natively is real, messy work with real limits
  -- reinforcing that this project's existing shape (rfork() stays the
  simple, real thing on Plan9; fork() is the only thing Unix gets, no
  attempt to emulate general rfork() flags there) is the safer choice,
  not just the lazier one.

- dirstat.c (category C): builds a Dir* from native lstat()/stat() via
  an internal marshaling helper (_p9dir()) -- independent corroboration
  that goken's own already-settled dirstat()/dirfstat() design (Dir*
  built directly from each GOOS's native stat data, no raw wire-format
  stat() exposed) is the same shape plan9port itself converged on, not
  a goken-specific choice.

Notes vs. signals: a possible middle ground
------------------------------------------------------------------------

UPDATE: both the Plan9 half (notify()/noted() as real syscalls,
atnotify()/postnote() built on top) and the Linux half (sigaction()/
kill()-based) of this are now DONE -- see notes_libc_selfhost.txt's
"Tier 6: notification, Plan9 side (arm/mips)" and "...Linux side, all
7 arches" entries. The proposal below undersold the Linux side's real
complexity in one respect: it correctly predicted the signal<->string
table and postnote()-via-kill() shape, but did not anticipate that
installing the dispatcher as sa.handler would need a per-arch bridging
trampoline at all (sigentry) -- found only once real per-arch testing
started, not something the design pass below could have caught by
reasoning alone. darwin's postnote() (kill()-based, no sigaction
involved) is ALSO now done -- but notify()/noted() there is not a
"not implemented yet" gap the way this proposal originally framed it;
see notes_libc_selfhost.txt's "Tier 6: notification, darwin side"
entry for why it turned out to be a genuinely unresolved DESIGN
question (XNU's real sigaction ABI, not a close cousin of Linux's the
way this proposal's Linux-side reasoning turned out to still mostly
hold), not just an implementation this session ran out of time for.
See plan_syscalls.txt's Tier 6 section for the tracked status.
Recorded here because the reasoning is the reusable part.

The friction is structural, not just naming: Plan9 notes are free text
("sys: trap: fault read addr=0x0", "alarm", "hangup", or literally
anything a program invents via postnote()); Unix signals are a closed
set of ~30 integers. This is lossier than fork->rfork ever was --
there is no total mapping in either direction. noted(NCONT/NDFLT/
NSAVE/NRSTR) also lets a handler tell the kernel exactly what to do
with the *saved fault state*; only NCONT/NDFLT have a clean POSIX
shape (return normally / restore default and re-raise). NSAVE/NRSTR
(park the fault state, resume it later) has no real POSIX equivalent.

The useful fact, from actually surveying real callers rather than
guessing: nothing in this tree calls raw notify() directly, and
nothing calls noted(NSAVE) or noted(NRSTR). Every real blocker
(mk/Plan9.c, utilities/pipe/tee.c, utilities/text/misc/ed.c) goes
through atnotify()/postnote()/noted(NCONT)/noted(NDFLT) only.

That splits the problem cleanly along the category-A/B line above:

- notify() itself (raw handler, Ureg*, kernel-delivered) -- keep
  Plan9-implemented-only for now, undiminished. See the precedent
  section below for why this is a SCOPE decision (nothing in this
  tree calls it directly), not a technical one -- it turns out to be
  buildable.
- atnotify()/postnote()/noted() are Plan9's OWN higher-level
  convenience layer on top of notify() -- principia's port/atnotify.c
  is already a portable NFN-slot dispatch table, not kernel magic.
  Category A/B territory, and the layer every real caller actually
  uses -- same shape as fork() being a convenience wrapper over the
  fully general rfork().

Real precedent: BOOT/lib9's own notify.c/await.c
------------------------------------------------------------------------

notify()/atnotify() specifically (see the general BOOT/lib9/ section
above): lib9's notify.c/atnotify.c already solve this exact problem,
in the exact library this project already treats as authoritative
reference elsewhere. Worth reading before designing this tier's real
implementation, not just the summary below. Two corrections to the
proposal above, both found by actually reading it rather than
re-deriving from first principles:

1. lib9 implements raw notify() directly on Unix -- it does NOT stop
   at atnotify(). BOOT/lib9/notify.c's signotify() is the sigaction
   handler for every signal in a curated table; it calls the single
   registered notifyf(nil, notestring) exactly like a Plan9 kernel
   would, then uses a per-notification jmp_buf (Jmp/_notejmpbuf, swap-
   able by a thread library) so that noted(v) -- called from deep
   inside notifyf, exactly like Plan9's own callback-then-tell-the-
   kernel-what-next contract -- can p9longjmp() straight back into
   signotify()'s own 3-way switch: case 0 is the fresh call, case 1 is
   noted(NDFLT) (apply the signal's default disposition and re-raise
   or _exit), case 2 is noted(NCONT) (just return from the handler).
   This is a genuinely elegant real answer to "how does noted() give
   the kernel an instruction" on a platform with no such kernel
   concept -- adopt this jmp_buf shape directly rather than re-
   inventing a weaker one (setjmp/longjmp already exists in this tree
   as of the Tier 7 work, so this is now buildable here too).
   atnotify() (BOOT/lib9/atnotify.c) is then EXACTLY principia's own
   NFN-slot dispatch table, calling notify(notifier) once and getting
   the chain for free -- confirms the port/atnotify.c-is-shareable
   assumption above is correct, not just plausible.

2. The void* argument notifyf receives is simply passed as nil on
   Unix (signotify() calls `(*notifyf)(nil, ...)`) -- nothing tries to
   synthesize a Ureg* from ucontext_t. So "notify() is hard to shim
   because of the Ureg* argument" was an overstated objection: nobody
   downstream (in lib9 OR in this tree's own atnotify()/tee.c/ed.c
   callers, which never touch the void*) needs it filled with anything
   real. The actual reason to leave notify() Plan9-only for now is
   simply that nothing in THIS tree calls it directly yet, not that it
   can't be done -- lib9's notify.c is a ready-to-adapt reference the
   moment something does.

The real, production-tested signal<->note-string table lives in
BOOT/lib9/await.c (~line 15-57, `_p9sigstr()`/`_p9strsig()`), not
invented here -- worth transcribing the shape rather than the exact
copy, since it will need retuning for goken's own signal availability
per GOOS (`#ifdef SIGEMT`/`SIGWINCH`/`SIGINFO` guards for BSD/macOS-
only signals already show in the original):

  SIGHUP->"hangup"      SIGINT->"interrupt"   SIGQUIT->"quit"
  SIGALRM->"alarm"      SIGTERM->"kill"       SIGKILL->"sys: kill"
  SIGILL->"sys: illegal instruction"      SIGTRAP->"sys: breakpoint"
  SIGABRT->"sys: abort"    SIGFPE->"sys: fp: trap"
  SIGBUS->"sys: bus error" SIGSEGV->"sys: segmentation violation"
  SIGPIPE->"sys: write on closed pipe"
  ...and a dozen more (SIGURG/STOP/TSTP/CONT/CHLD/TTIN/TTOU/IO/XCPU/
  XFSZ/VTALRM/PROF/WINCH/INFO/USR1/USR2), all "sys: ..."-prefixed.
  Unmapped signal numbers fall back to a generated "sys: signal %d"
  string; unmapped note strings make _p9strsig() return 0 (no signal).

Two corrections to what was speculated in the first draft of this
proposal: no fault-address detail (si_addr) is folded into the
SIGSEGV/SIGBUS strings the way a real Plan9 kernel note would --
lib9 keeps it to a fixed generic string per signal, and there is no
real precedent for going further than that, so don't. And SIGTERM and
SIGKILL map to two DIFFERENT strings ("kill" vs "sys: kill") -- easy
to collapse into one by accident when transcribing, don't.

notify.c also implements noteenable()/notedisable()/notifyon()/
notifyoff() (sigprocmask()/sigaction()-based selective signal
masking) -- goken's own include/os/plan9/note.h already has these
commented out at the bottom, apparently anticipating exactly this.
Not needed for any current blocker (mk/tee/ed don't call them) --
resurrect only if a real caller shows up, same "don't build a door
nobody's knocking on" discipline as category C/D above.

Revised concrete plan, if/when this gets built:
1. port/atnotify.c ported once (from principia, per the existing
   Tier 6 plan), shared UNCHANGED across every GOOS -- confirmed
   shareable by lib9's own atnotify.c being effectively the same file.
2. Linux/darwin delivery glue modeled directly on BOOT/lib9/notify.c:
   sigaction() over a curated signal table, a per-notification
   jmp_buf (this tree's own setjmp/longjmp, Tier 7) standing in for
   lib9's p9jmp_buf, noted(v) implemented as
   `longjmp(jb, v==NCONT ? 2 : 1)`, notifyf always called with nil for
   the void* argument.
3. The signal<->string table above, adapted (not copied verbatim) for
   goken's own per-GOOS signal availability.
4. postnote(PNPROC/PNGROUP, pid, msg) -> reverse-lookup msg in the
   same table -> kill(pid, sig) / kill(-pid, sig). This direction is
   NOT lossy -- PNPROC/PNGROUP already ARE kill(pid,.)/kill(-pgid,.),
   a direct translation, not a compromise.
5. Document the one real, un-fixable limitation: an arbitrary/uncommon
   note string (anything postnote() sends that isn't in the table) has
   no signal to map to. lib9 doesn't solve this either -- Plan9's
   free-text notes are strictly richer than Unix's closed signal set,
   and no shim erases that, only bounds it to the common cases real
   code actually exercises.

This keeps callers (tee.c, ed.c, a future self-hosted mk) writing the
exact same atnotify()/postnote()/noted() calls on every GOOS, does not
need a goken.c-style whole-file swap for this particular piece -- the
Plan9-vs-Unix difference is small enough to isolate into just the
delivery-glue layer, the same way darwin's fork.c/exec.c/wait.c/pipe.c
turned out reusable unchanged from linux and only the raw syscall stub
needed per-OS asm (see notes_libc_selfhost.txt's Tier 4 round 2 entry)
-- and, per the precedent above, would extend cleanly to a real
notify() implementation later without redesigning anything, since
that's the one piece BOOT/lib9/notify.c proves out already.

If a design session ever happens for this, doing the /env-vs-environ
question (category F above) in the same pass is worth considering --
same shape of problem, real callers already waiting (mk/rc), likely a
per-GOOS glue file rather than a goken.c-sized rewrite either way.

------------------------------------------------------------------------
spawn(): a portable process-spawn primitive, not fork() everywhere
------------------------------------------------------------------------

The gap this closes: mk/goken.c's execsh()/pipecmd() and rc/processes.c's
Xasync() are all hand-rolled fork()+dup()+exec() sequences (see
notes_libc_selfhost.txt's mk/rc self-hosting entries) -- portable across
linux/darwin/plan9, where fork() already has a uniform, flag-free
signature (port/fork.c's raw syscall wrapper on linux/darwin, os/plan9/
fork.c's rfork(RFPROC|RFFDG|RFREND) on plan9), but fundamentally
inexpressible on windows: there is no fork() there at all, only the
atomic CreateProcess(app, cmdline, ..., startupinfo, procinfo), which
combines what fork()+dup()+exec() do across three separate calls into
one. Getting mk/rc working on windows means SOMETHING has to bridge that
gap, and doing it once in libc (rather than per-caller, per-GOOS
branching inside mk/goken.c and rc/processes.c both) is the same
"portable API over incompatible primitives" shape environ()/putenv()
already settled for the environment (see this file's own "Environment"
section) and notify()/noted() partially settled for signals above.

The chosen shape:
    int spawn(char *path, char **argv, fdt fdin, fdt fdout, fdt fderr);
Starts `path` as a new process running `argv`, with its stdin/stdout/
stderr connected to the given fds (-1 meaning "leave mine as they are").
Returns immediately (does not wait) with a pid wait()/waitpid() can
later be given, or -1 on failure. Deliberately does NOT feed stdin or
drain stdout itself -- the caller builds a pipe, hands one end to
spawn(), and reads/writes the other end itself, exactly the division of
labor fork()+dup()+exec() already has. This is narrower than POSIX's own
posix_spawn() (which takes a whole posix_spawn_file_actions_t list of
open/close/dup2 steps) -- three fixed fd slots is all any real caller in
this tree (mk, rc) has ever needed, and a narrower primitive is easier
to implement honestly on windows than a general file-actions list would
be.

This is NOT a novel idea invented for this tree -- it is the same
convergent answer essentially every language/library that has to be
portable across POSIX and Windows has landed on, precisely because
Windows never had fork() as a first-class concept at all:
  - POSIX itself standardized posix_spawn()/posix_spawnp() (POSIX.1-2008)
    for a related but distinct reason -- fork() is unsafe in threaded or
    memory-constrained contexts (only async-signal-safe calls are legal
    between fork and exec) -- but the shape (atomic spawn, fd redirection
    described declaratively, no bare fork exposed) is the same idea.
  - libuv (the C library under Node.js) has uv_spawn() -- literally the
    same verb -- taking a uv_process_options_t with stdio redirection,
    implemented as fork+exec on POSIX and CreateProcess on Windows behind
    one API.
  - Rust's std::process::Command: Command::new(path).stdin(...).spawn()
    returns a Child you .wait() on -- near-identical shape, same verb,
    down to the method name.
  - Go's os/exec, Python's subprocess.Popen, Java's ProcessBuilder: same
    convergent design, hiding fork() (or CreateProcess) behind one
    portable "start a process with these fds wired up, get back a
    waitable handle" API.
So this project's own spawn() is a small, Plan9-styled member of an
already well-established design family, not a one-off invention.

Per-GOOS implementation:
  - linux/darwin/plan9: port/spawn.c, one portable file, no GOOS
    branching -- fork(), dup() the given fds onto 0/1/2 in the child
    (closing the original if it wasn't already at that number, matching
    mk/goken.c's own already-verified execsh()/pipecmd() idiom), exec(),
    _exits("exec") if exec() itself failed. Exactly the pattern mk/rc
    already hand-roll, factored into one reusable call.
  - windows: os/windows/spawn.c, genuinely new -- CreateProcess with
    STARTUPINFOA.dwFlags=STARTF_USESTDHANDLES and hStdInput/hStdOutput/
    hStdError wired to the given fds (real byte offsets computed from
    MinGW-w64's own processthreadsapi.h, not guessed, same discipline as
    every other Windows struct-offset gap this tree has hit). Since
    os/windows/pipe.c's own CreatePipe call creates NON-inheritable
    handles by default (its own header comment), the child-facing handle
    needs SetHandleInformation(..., HANDLE_FLAG_INHERIT, 1) right before
    CreateProcessA -- otherwise bInheritHandles=TRUE has nothing
    inheritable to actually hand the child. Returns without waiting, the
    same contract as the POSIX/plan9 side -- CreateProcess is already
    non-blocking, so windows spawn() needs no fork()-alike helper process
    to achieve that, unlike what a naive port might assume.
  - wait()/waitpid() need their own windows implementation to pair with
    this: unlike POSIX wait4(-1,...), Win32 has no "wait for any child"
    primitive at all, only WaitForSingleObject (one handle) or
    WaitForMultipleObjects (an explicit list, capped at
    MAXIMUM_WAIT_OBJECTS=64). So windows wait() needs a small table of
    in-flight child HANDLEs (populated by spawn(), drained by wait()) to
    build that list from -- the same "keep a real table instead of
    assuming a truncated int is enough" idea todo.org's own windows
    dup() gap already flagged as the eventual right fix for fd-as-HANDLE
    truncation, just scoped to processes instead of general fds.

Once this exists, mk/goken.c's execsh()/pipecmd() and rc's own spawn
call sites become callers of one portable primitive instead of each
carrying their own fork()-vs-CreateProcess branch -- see
notes_libc_selfhost.txt for the concrete rollout and verification
status.


------------------------------------------------------------------------
alarm(): where the "extend the API" argument does NOT apply
------------------------------------------------------------------------

alarm(ms) -- Plan9's millisecond one-shot timer, posting an "alarm" note
on expiry -- had been declared in include/os/time.h from the start with
no implementation behind it on any GOOS. It now has one everywhere it
can have one: a direct syscall on plan9 (syscall/os/plan9/svc_
$cputype.s, using sys.h's ALARM which was already defined and unused),
setitimer(ITIMER_REAL) glue on linux and darwin (os/$GOOS/alarm.c), and
a documented always--1 on windows.

The design question worth recording is the one that came WITH the
request: should a more general, setitimer()-shaped API go in alongside
it, the way spawn() went in alongside (and above) fork()? The tree does
extend the Plan9 surface when there is a reason to, so this is a fair
question rather than a settled one.

The answer here is no, and the reason is that spawn()'s argument runs
the opposite direction. spawn() earned its place by being MORE portable
than what it replaced: fork()+dup()+exec() is fundamentally
inexpressible on windows (see the section above), so a new primitive was
the only way to cover all four GOOSes at all. A general interval timer
is the reverse trade -- it is more EXPRESSIVE but less portable:

  - plan9's kernel offers this one-shot alarm and nothing else. There is
    no setitimer, no timer_create, no interval timer of any kind. A
    general API would have to be emulated in userspace on the very GOOS
    whose API this is, and with no threads in this libc there is nothing
    to run that emulation on.
  - windows cannot even back alarm() today, because the missing piece is
    not the timer (Win32 has several) but note delivery, which this GOOS
    has none of. A richer timer API would be more surface with strictly
    less behind it.
  - the expressiveness actually missing -- a REPEATING timer -- is the
    part a caller can supply for itself portably, by re-arming from its
    own note handler. That works on plan9 too, which a setitimer()
    wrapper would not.
  - and there is no caller. Nothing in this tree calls alarm() yet, let
    alone wants an interval one. Adding surface ahead of a caller is the
    thing this file's own "Plan9-clean primitives" framing argues
    against, and the same restraint port/putenv.c's header comment
    already applies to unsetenv().

What was done instead, as a cheap hedge: the RAW setitimer stub is
generated on both linux and darwin (_syssetitimer in each .decl), since
that is where the per-arch syscall-number work actually lives. If a real
caller ever needs interval timers, the plumbing is already in place on
the two GOOSes that could support them, and only the portable-surface
question is left open -- which is the right thing to leave open, because
it is a question about callers, not about syscalls.

One implementation note worth keeping, since it is the kind of thing
that silently half-works: linux uses setitimer rather than the alarm(2)
syscall, and not as a preference. alarm(2) does not exist on the generic
syscall ABI (arm64, riscv, riscv64) -- dropped with the rest of the
legacy group, exactly as fork() and pipe() were in favour of clone() and
pipe2(). It also takes whole SECONDS, so it could not honour this
signature's millisecond promise anywhere, even on the arches that still
have it. setitimer is present on all seven and is millisecond-capable,
so one glue file covers them all.
